interface:
  display_name: "TDD"
  short_description: "Write failing stage tests first."
  default_prompt: "Use $idea-to-ship:tdd to create stage-local failing tests before implementation or backfill missing tests."
