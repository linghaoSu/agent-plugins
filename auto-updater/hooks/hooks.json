{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -lc 'for root in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${CODEX_PLUGIN_ROOT:-}\" \"${PLUGIN_ROOT:-}\" \"$PWD\"; do [ -x \"$root/scripts/check-update.sh\" ] && exec \"$root/scripts/check-update.sh\"; done; exit 0'"
          }
        ]
      }
    ]
  }
}
