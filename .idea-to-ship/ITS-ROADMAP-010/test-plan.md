# Test Plan - ITS-ROADMAP-010

## Stage TDD Slices

| Stage | Story | Acceptance | Scenario | Test | Expected Initial Result | Command |
|---|---|---|---|---|---|---|
| Stage 1 | Skill maintainer gets deterministic staged hygiene checks | Staged checker reads selected skill content from the index even when the worktree file is absent | Staged modified skill exists only in index and has a long description | TDD-1 | fail: current staged discovery filters the deleted worktree path and misses `long-description` | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 1 | Plugin releaser can trust staged new-skill metadata checks | Staged added-skill metadata detection uses the index, not the worktree | Staged added skill exists only in index and has no sibling metadata | TDD-2 | fail: current added-skill discovery filters the deleted worktree path and misses `missing-openai-metadata` | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 1 | Reviewer sees old hygiene checks preserved while snapshot logic changes | Existing check IDs keep firing in deterministic temporary repos | All-mode long description, inline output contract, oversized skill, working added metadata, and staged index-not-worktree cases | TDD-3 | pass or fail only if existing behavior regresses; included as guardrail around the red staged cases | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 2 | Plugin releaser sees checker fixture coverage in the release gate | Release gate exposes `skill-hygiene-fixtures` as an advisory in all mode | `scripts/release-gate.sh --mode all --json` includes a passing `skill-hygiene-fixtures` check | TDD-4 | fail: current release gate has no `skill-hygiene-fixtures` advisory | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 2 | Release-gate fixture harness is protected without recursion | Release gate exposes a non-recursive `skill-hygiene-release-gate-fixtures --self-check` advisory | `scripts/release-gate.sh --mode all --json` includes a passing `skill-hygiene-release-gate-fixtures` check and the self-check command is wired | TDD-5 | fail: current release gate has no `skill-hygiene-release-gate-fixtures` advisory or self-check command | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 2 | Plugin releaser can trust mode-specific fixture behavior | Fixture advisories pass, skip, warn, and strict-upgrade deterministically | Full release-gate fixture creates a candidate temp repo and verifies staged skip, working pass, working warn, and working strict failure | TDD-6 | fail until the new advisory IDs and commands are wired; then guards future regressions | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 3 | Skill maintainer can inspect prompt/template candidate inventory without enabling findings | Candidate inventory exposes prompt/template family, line spans, lengths, placeholder ratio, stable anchors, fingerprint, and output-contract masking status | Temporary repo contains one prompt-like block and one template-like report wrapper | TDD-7 | fail: checker has no fixture-visible candidate inventory output yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 3 | Skill maintainer avoids ordinary-section false positives | Ordinary `## Requirements`, `## Architecture`, and `## Final Report` sections without prompt/template triggers produce no candidate inventory records and no normal hygiene findings | Temporary repo contains ordinary sections that only reference external behavior | TDD-8 | fail: checker has no candidate inventory output yet; implementation must keep this output empty for ordinary sections | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 3 | Reviewer can verify non-fenced prompts keep internal headings intact | Candidate extraction spans internal allowed headings after a prompt trigger | Non-fenced prompt includes internal `## Requirements`, `## Architecture`, and `## Final Report` sections | TDD-9 | fail: checker has no candidate inventory output yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 4 | Skill maintainer can baseline exact repetition before enabling warnings | `--dry-run-repetition-baseline` reports exact prompt/template matches with line-oriented structural fields and exits 0 without normal findings | Temporary repo contains same-file prompt duplication and cross-file template duplication | TDD-10 | fail: checker has no `--dry-run-repetition-baseline` flag or exact-match baseline output | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 4 | Reviewer can trust output-contract masking before repeated-inline checks are enabled | Repeated output-contract-only blocks are not counted as prompt/template repetition, while repeated prompt bodies with embedded output contracts still baseline as prompt matches | Temporary repo contains duplicated output-contract-only block and duplicated prompt-with-contract block | TDD-11 | fail: checker has no contract subspan masking or baseline match output | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 5 | Skill maintainer sees moderate bloat before the hard oversized threshold | A 401-line skill emits `moderate-skill-bloat` with line count, threshold, and extraction/shared-contract guidance | Temporary repo contains 401-line skills with no exception, valid exception, empty exception, unrelated exception, and >750-line exception cases | TDD-12 | fail: checker has no `moderate-skill-bloat` ID or exception contract yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 5 | Plugin releaser sees moderate bloat through existing advisory strict-upgrade behavior | Release gate reports `WARN skill-hygiene` for a 401-line skill in non-strict mode and `FAIL skill-hygiene` with `--strict` | Candidate temp repo injects a 401-line skill outside the fixture trigger scope | TDD-13 | fail: release gate has no moderate-bloat hygiene warning to upgrade yet | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 6 | Skill maintainer sees exact same-file repeated prompts | Same-file exact duplicated prompt-like blocks emit one `repeated-inline-prompt` finding with duplicate count and extraction guidance | Temporary repo contains one skill with two exact prompt candidates | TDD-14 | fail: checker has exact baseline matches but no normal repeated prompt finding yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 6 | Skill maintainer sees copied prompt blocks in changed targets | Working-mode target skills copied from existing reference skills emit `repeated-inline-prompt` regardless of path sort order | Temporary repo has committed prompt sources and untracked target copies that sort before and after their sources | TDD-15 | fail: checker does not emit repeated prompt findings for cross-file copies yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 6 | Plugin releaser gets strict-upgrade behavior for repeated prompts | Release gate reports `WARN skill-hygiene` for a repeated prompt in non-strict mode and `FAIL skill-hygiene` with `--strict` | Candidate temp repo injects one repeated-prompt skill outside the fixture trigger scope | TDD-16 | fail: release gate has no repeated-prompt hygiene warning to upgrade yet | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 7 | Skill maintainer sees near-duplicate repeated prompts | Same-file prompt candidates above the fuzzy similarity threshold emit `repeated-inline-prompt` with `same-file-fuzzy` evidence and appear in dry-run baseline output | Temporary repo contains two near-identical prompt candidates in one skill | TDD-17 | fail: checker has no fuzzy prompt matching yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 7 | Skill maintainer sees bounded scan-limit warnings only for plausible prompt pairs | Pair-cost-limited prompt candidates emit `repetition-scan-limited`; valid scan-limit exception suppresses only that ID; exact duplicates still emit; lone large candidate stays clean | Temporary repo contains limited, excepted, and lone-large prompt samples | TDD-18 | fail: checker has no fuzzy budget or scan-limit finding yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 7 | Plugin releaser gets strict-upgrade behavior for scan-limit warnings | Release gate reports `WARN skill-hygiene` for scan-limited prompt candidates in non-strict mode and `FAIL skill-hygiene` with `--strict` | Candidate temp repo injects one scan-limited prompt skill outside the fixture trigger scope | TDD-19 | fail: release gate has no scan-limit hygiene warning to upgrade yet | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 8 | Skill maintainer sees exact repeated templates | Exact duplicated template/report-wrapper blocks emit `repeated-inline-template` with duplicate count and extraction guidance | Temporary repo contains same-file and working-mode cross-file template duplicates | TDD-20 | fail: checker can baseline exact template matches but does not emit template findings yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 8 | Plugin releaser gets strict-upgrade behavior for repeated templates | Release gate reports `WARN skill-hygiene` for a repeated template in non-strict mode and `FAIL skill-hygiene` with `--strict` | Candidate temp repo injects one repeated-template skill outside the fixture trigger scope | TDD-21 | fail: release gate has no repeated-template hygiene warning to upgrade yet | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Stage 9 | Skill maintainer sees near-duplicate repeated templates | Same-file template candidates above the fuzzy similarity threshold emit `repeated-inline-template` with `same-file-fuzzy` evidence and appear in dry-run baseline output | Temporary repo contains two near-identical template candidates in one skill | TDD-22 | fail: checker has no fuzzy template matching yet | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 9 | Skill maintainer sees bounded scan-limit warnings for template pairs | Pair-cost-limited template candidates emit `repetition-scan-limited`; valid scan-limit exception suppresses only that ID; exact duplicates still emit; lone large template candidate stays clean | Temporary repo contains limited, excepted, and lone-large template samples | TDD-23 | fail: scan-limit applies only to prompt candidates | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 9 | Plugin releaser gets strict-upgrade behavior for template scan-limit warnings | Release gate reports `WARN skill-hygiene` for scan-limited template candidates in non-strict mode and `FAIL skill-hygiene` with `--strict` | Candidate temp repo injects one scan-limited template skill outside the fixture trigger scope | TDD-24 | fail: release gate has no template scan-limit hygiene warning to upgrade yet | `bash tests/skill-hygiene-release-gate-fixtures.sh` |

## Backfill Test Slices

| Source | Gap | Scenario | Test | Expected Result | Command |
|---|---|---|---|---|---|
| Stage 3 code review | Classifier scoring boundary coverage was too narrow | Prompt with structured output stays `prompt`; template-dominant output, output-only input sections, placeholder-label templates, and structured `## Output` wrappers stay `template`; ordinary labeled final-report prose stays out of candidate inventory | BF-1 | review-backfill: failed until classifier boundary fixtures and weighted scoring matched architecture | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 3 code review | Allowlisted internal headings could absorb ordinary prose | Prompt trigger followed by ordinary `## Requirements` prose, including ordinary `Scope:` labels, stops before the heading unless nearby structure justifies including it | BF-2 | review-backfill: failed until internal-heading lookahead required placeholder/table/known-YAML/output-instruction structure | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 3 code review | Normalization did not prove line-number and uppercase-placeholder equivalence | Numbered vs unnumbered prompts fingerprint equally; `SOURCE_PATH` vs `TARGET_PATH` uppercase placeholders fingerprint equally | BF-3 | review-backfill: failed until line-number prefixes and uppercase placeholders were normalized before fingerprinting | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 4-10 code review | Hygiene exceptions could be suppressed by fenced examples | Fenced `## Hygiene Exception` text does not suppress `moderate-skill-bloat` or `repetition-scan-limited` | BF-4 | review-backfill: failed until hygiene exception parsing became line-anchored, markdown-section based, and fence-aware | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 7/9 code review | Scan-limit diagnostics could report `pair_cost=0` and omit whole-run caps | Pair-cost-limited findings report nonzero pair cost; prompt-heavy whole-run budget exhaustion emits `repetition-scan-limited` without starving template fuzzy checks | BF-5 | review-backfill: failed until attempted pair cost and per-family total budgets were tracked | `bash tests/skill-hygiene-check-fixtures.sh` |
| Stage 9 code review | Release-gate template scan-limit fixture did not create a near-duplicate | Template scan-limit release-gate fixture changes `issue alpha` to `issue beta` and asserts JSON evidence contains `repetition-scan-limited` | BF-6 | review-backfill: failed until fixture substitution and JSON evidence assertions were corrected | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| FR-5 code review | Positive release-gate coverage did not prove staged/all hygiene warning paths | New hygiene findings flow through `skill-hygiene` in working, all, and staged modes, including a staged index-only/worktree-deleted sample | BF-7 | review-backfill: failed until full release-gate fixture added all-mode and staged-mode positive warning assertions | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 2 code review | Hygiene exception parsing still allowed non-contract headings and fenced lines inside a real section | Only a real level-2 `## Hygiene Exception` outside fences can suppress; fenced examples inside or outside the section and `### Hygiene Exception` do not suppress | BF-8 | review-backfill: failed until exception section parsing required level 2 and stripped fenced blocks before matching keys/evidence | `bash tests/skill-hygiene-check-fixtures.sh` |
| Iteration 2 code review | Output-contract masking did not prove marker ownership by same fence/section | Markers split across unrelated sections do not create an owned output-contract span; same-section markers still mask | BF-9 | review-backfill: failed until output-contract span detection grouped markers by fence/markdown-section owner | `bash tests/skill-hygiene-check-fixtures.sh` |
| Iteration 2 code review | Whole-run budget evidence lacked per-family total counters | `repetition-scan-limited` includes total comparison, character, and pair-cost counters for total-budget limits | BF-10 | review-backfill: failed until `ScanLimit` carried total counters and fixture asserted total evidence fields | `bash tests/skill-hygiene-check-fixtures.sh` |
| Iteration 3 code review | Dry-run baseline and fixtures did not prove whole-run budget totals strongly enough | Dry-run summary includes total counters, the scaled fixture parses numeric totals and checks caps/nonzero values, and release-gate positives cover moderate/scan-limit in staged/all modes | BF-11 | review-backfill: failed until dry-run summaries exposed totals and release-gate fixtures added all/staged moderate and scan-limit warning paths | `bash tests/skill-hygiene-check-fixtures.sh && bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 4 code review | Hygiene exceptions still trusted tilde-fenced and indented-code examples; template whole-run budget lacked a family-exhaustion proof | Tilde fences and four-space indented examples cannot suppress `moderate-skill-bloat` or `repetition-scan-limited`, including inside a real exception section; template-heavy scaled fixture emits `reasons=total_pair_cost` with under-cap totals | BF-12 | review-backfill: failed until Markdown code-block parsing handled tilde fences and indented code, and the whole-run fixture exhausted the template family budget independently | `bash tests/skill-hygiene-check-fixtures.sh` |
| Iteration 5 code review | Architecture-required staged infrastructure drift guard was missing; exact matching and fixtures were weaker than the design requires | Staged release gate fails blocking `skill-hygiene-infra-drift` in JSON when staged canonical hygiene infrastructure differs from dirty worktree infrastructure; exact cross-file repetition uses grouped exact indexes; exact-template same-file fixture asserts a finding line for the same-file path; prompt whole-run budget fixture discovers the capped prompt path dynamically | BF-13 | review-backfill: failed until the staged drift guard and fixture were implemented, exact matching stopped scanning the full reference corpus per target, and fragile fixture assertions were tightened | `bash tests/skill-hygiene-check-fixtures.sh && bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 6 code review | Staged drift guard missed untracked canonical infra paths, lacked ordinary-skill negative coverage, and the release-gate fixture exceeded the original runtime target | Untracked `tests/skill-hygiene-*` files fail `skill-hygiene-infra-drift` when infra is staged; staged ordinary skill changes with dirty infra skip the drift guard; dry-run summaries expose exact-index grouping metrics; the historical reduced-matrix runtime target was later superseded by BF-21's accepted split between fast release-gated `--self-check` and about 60s for the explicit full meta-test | BF-14 | review-backfill: failed until untracked drift was included, negative staged-skill coverage was added, exact-index metrics were asserted, and the reduced fixture ran in `23.670s`; BF-21 supersedes the full-fixture under-30s expectation after matrix restoration | `bash tests/skill-hygiene-check-fixtures.sh && time bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 7 code review | Hidden HTML-comment exceptions, mixed fence markers, exact duplicate group fan-out, and release-gate fixture coverage gaps remained | HTML-commented `## Hygiene Exception` examples cannot suppress bloat or scan-limit findings; fenced candidate and output-contract handling respects opening marker type; cross-file exact matching emits representative matches carrying duplicate counts; release-gate fixture covers moderate bloat, repeated prompt, scan-limit, self-check failure, ordinary staged skill dirty-infra skip, and untracked infra drift; BF-21 later supersedes the historical under-30s full-fixture target with an accepted about-60s explicit meta-test runtime | BF-15 | review-backfill: failed until comments were treated as invisible, fence scanning became marker-aware, exact matches carried representative duplicate counts, and release-gate fixtures split hygiene strict evidence from self-check failure evidence; BF-21 records the current runtime contract | `bash tests/skill-hygiene-check-fixtures.sh && time bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 8 code review | Inline HTML comments, staged index-only release-gate proof, scan-limit family aggregation, output-contract marker-aware masking, and candidate harness pathspec drift remained | Inline hidden exception reason/evidence does not suppress findings; fenced comment markers do not hide later visible exceptions; staged scan-limit release-gate proof reads index content after worktree deletion; one path can aggregate `families=prompt,template`; tilde fenced output contracts with inner backtick markers stay masked; candidate repos copy all canonical `tests/skill-hygiene-*` files | BF-16 | review-backfill: failed until inline comments were stripped before matching keys/evidence, staged release-gate fixture deleted the worktree skill after `git add`, scan-limit aggregation was covered, output-contract marker-aware masking was covered, and candidate harness expanded the canonical wildcard pathspecs | `bash tests/skill-hygiene-check-fixtures.sh && time bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 9 code review | Template release-gate evidence, same-marker info-string fences, placeholder-heavy exact templates, fenced output-contract subspans, and HTML-commented contract markers remained underproved | Release-gate JSON evidence contains repeated-template and template scan-limit findings without losing later findings to truncation; a backtick fence line with an info string does not close the active fence; placeholder-heavy templates remain exact-only candidates; fenced prompt bodies before output contracts still match; HTML-commented contract markers do not mask visible prompt evidence | BF-17 | review-backfill: failed until skill-hygiene evidence preserved full finding lines, closing fences required blank rest text, high-ratio templates were admitted as exact-only, fenced contract masking started at the contract subspan, and output-contract scanning ignored HTML comments outside fences | `bash tests/skill-hygiene-check-fixtures.sh && time bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 10 code review | Exact-only cross-file matching, ordinary-heading contract field masking, weak scan-limit exception evidence, skill-hygiene evidence truncation, and fixture infra target drift remained | Cross-file exact-only templates with equal fingerprints but different stable anchors do not warn; heading-started prompts with `status`/`truncated` fields keep the prompt body visible; `reviewed-with: ok` does not suppress scan-limit warnings; release-gate skill-hygiene evidence preserves all finding lines; release-gate fixture self-check compares its canonical pathspec list against `scripts/release-gate.sh` | BF-18 | review-backfill: failed until cross-file exact matches reused the exact-only anchor guard, output-contract heading rewind was limited to the real output-contract heading, scan-limit exceptions required dry-run/fixture evidence tokens, evidence joining removed hard truncation, and the fixture added an infra-target list self-check | `bash tests/skill-hygiene-check-fixtures.sh && time bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Iteration 11 code review | All-mode canonical selection still used raw exact groups before exact-only anchor partitioning | Placeholder-heavy exact-only all-mode matching treats each stable-anchor subgroup independently, so a three-template fixture with one anchor-A template and two anchor-B templates warns only for the non-canonical anchor-B copy | BF-19 | review-backfill: failed until exact candidate grouping included stable anchors for exact-only candidates | `bash tests/skill-hygiene-check-fixtures.sh` |
| Re-review iteration 12 | Strict-upgrade fixture coverage did not prove moderate bloat or prompt-family scan-limit strict failures | Working strict release-gate fixture includes `moderate-skill-bloat`, repeated prompt/template, and both prompt/template scan-limit evidence in one strict-upgrade run | BF-20 | review-backfill: failed until working strict JSON assertions covered `moderate-skill-bloat`, `families=prompt`, and `families=template` evidence | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
| Re-review iteration 14 | Fixture-advisory matrix coverage was partially lost while reducing release-gate fixture runtime | Full release-gate fixture proves staged skip, working pass, non-strict warn, and strict failure for `skill-hygiene-fixtures` and `skill-hygiene-release-gate-fixtures`; architecture accepts about 60s for this explicit meta-test while keeping release-gated `--self-check` fast | BF-21 | review-backfill: failed until focused JSON assertions restored the TDD-6 pass/skip/warn/strict matrix and the explicit runtime tradeoff was documented | `bash tests/skill-hygiene-release-gate-fixtures.sh` |
