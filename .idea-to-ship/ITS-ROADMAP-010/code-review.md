# Code Review - ITS-ROADMAP-010

**Date:** 2026-05-15
**Reviewer:** multi-agent: correctness/security -> Godel/Zeno/Kierkegaard/Tesla/Halley; traceability/testability -> Aristotle/Boole/Archimedes/Kepler/Huygens; maintainability/repo-fit -> Ampere/Popper/Mill/Raman/Turing; UI/UX -> not applicable
**Iterations:** 5
**Result:** clean
**Mode:** multi-agent
**Degradation reason:** none
**Diff size:** Stage 1-3 implementation/test/doc files plus ITS artifacts; tracked diff `3 files changed, 544 insertions(+)`, with 3 untracked fixture files reviewed from `git status --short`

## Issues Raised & Resolution

| # | Severity | File:line | Issue | Resolution |
|---|---|---|---|---|
| 1 | warning | `tests/skill-hygiene-release-gate-fixtures.sh:73` | Temporary dirs used a space-delimited scalar, unsafe for paths with spaces. | Switched to a Bash array plus explicit count guard and quoted `rm -rf --`. |
| 2 | warning | `tests/skill-hygiene-release-gate-fixtures.sh:162` | Release-gate fixture warn/strict coverage only exercised `skill-hygiene-fixtures`. | Added a separate self-check warn/strict candidate for `skill-hygiene-release-gate-fixtures`. |
| 3 | warning | `tests/skill-hygiene-check-fixtures.py:112` | All-mode fixture did not prove committed legacy skills without metadata stay clean. | Added committed legacy no-metadata all-mode fixture. |
| 4 | warning | `tests/skill-hygiene-check-fixtures.py` | Stage 3 classifier boundary coverage was too narrow. | Added prompt/template boundary, placeholder-ratio, single-signal, output-wrapper, and ordinary-label negative fixtures. |
| 5 | warning | `scripts/skill-hygiene-check.py` | Internal headings could absorb ordinary prose. | Added structure lookahead and narrowed YAML-like labels to known skeleton keys or placeholder labels. |
| 6 | warning | `scripts/skill-hygiene-check.py` | Candidate normalization did not strip line numbers or normalize uppercase placeholders before fingerprinting. | Added line-number prefix stripping and placeholder normalization before lowercasing. |
| 7 | warning | `scripts/skill-hygiene-check.py` | Prompt/template scoring drifted from the architecture table. | Split weighted scoring helpers for prompt roles, phrases, input placeholders, output headings, and skeleton signals. |
| 8 | warning | `.idea-to-ship/ITS-ROADMAP-010/test-plan.md` | Review-driven fixture additions were not reflected in artifacts. | Added backfill rows and updated `tdd-log.md` / `implementation-log.md`. |
| 9 | warning | `scripts/__pycache__/...` | Generated bytecode appeared during verification. | Removed generated `__pycache__` directories before final review. |

## Out-of-Scope Issues Skipped

None.

## Design Drift

None for completed Stage 1-3 scope. The only interface nuance is documented: Stage 3 uses hidden `--dump-repetition-candidates` fixture output, while the public `--dry-run-repetition-baseline` remains deferred to Stage 4 as designed.

## Test Traceability

Clean for Stage 1, Stage 2, and Stage 3. `test-plan.md` now records Stage TDD slices plus review-backfill slices for classifier boundaries, internal-heading structure, line-number normalization, uppercase-placeholder normalization, output-wrapper starts, and ordinary labeled prose negatives. Normal mode still emits no new hygiene findings.

Verification rerun during review:

- `bash tests/skill-hygiene-check-fixtures.sh` passed.
- `bash tests/skill-hygiene-release-gate-fixtures.sh` passed.
- `bash tests/skill-hygiene-release-gate-fixtures.sh --self-check` passed.
- `python3 scripts/skill-hygiene-check.py --mode all .` passed.
- `scripts/release-gate.sh --mode all --strict` passed.
- `scripts/release-gate.sh --mode working --strict` passed.
- `python3 secret-scanner/scripts/scan.py --mode working --format json` returned `[]`.
- `git diff --check HEAD` passed.
- `find scripts tests -name __pycache__ -type d -maxdepth 3` returned no paths.

## Residual Open Issues

None for Stage 1-3. Remaining ITS-ROADMAP-010 stages 4-10 are not implemented yet.

## Final Verdict

| Angle | Verdict |
|---|---|
| correctness/security | LGTM |
| traceability/testability | LGTM |
| maintainability/repo-fit | LGTM |
| UI/UX | not applicable |
